schema: 1

cost:
  # Repository-wide production policy. Keep generated/stock media spend at zero.
  target_incremental_media_cost_usd: 0
  paid_media_generation: false

soundtrack:
  # Paid music generation (including Lyria) is blocked while paid_media_generation=false.
  prefer_local_or_cc0_music: true
  prefer_cc0_sfx: true
